# CPU 开发环境依赖（本地直接运行用）
# 打包安装请用 pip install -e ".[cpu]" 或 install_gpu.bat / install_gpu.sh

# 核心
numpy>=1.24
opencv-python>=4.8          # 本地开发用（带 GUI）；服务器部署改为 opencv-python-headless
Pillow>=10.0

# OCR 引擎（CPU 版）
# GPU 版请运行 install_gpu.bat 或 install_gpu.sh
paddlepaddle>=3.0.0b2
paddleocr>=2.8.0
