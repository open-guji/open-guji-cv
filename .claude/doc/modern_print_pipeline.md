# 现代印刷链 `modern_body`：设计、配置与北行日錄实测

> 2026-09-14 立。设计正本在 overview 仓 `项目进展/图片初步数字化/进度/总览/05-三模式管线方案.md`
> （三模式：古籍 / 现代竖排 / 现代横排），本篇记**已落地的部分**与第一本书的实测。
> 横排（原图入口旋转 90°）**尚未接入**，`BookSpec.writing_mode` 只登记不生效。

## 一、一句话

九步一步不删。现代印刷本与刻本的差别只在切分线前三步，各用**新 Step 文件**顶替
（不动切分侧会话正在改的 `border_detect / column_warp / row_segment`，刻本链产物指纹不受影响），
产**同一套产物种类**，Step4 起原样复用：

| 步 | 刻本链 | 现代链 | 差别 |
|---|---|---|---|
| 0 | `preclean`（按需） | **`guji split`**（分页，按需） | 一张扫描页拼两页原书时先裁成逻辑页 |
| 1 | `border_detect`：找版框墨线 | **`line_detect`**：找墨柱 | 无版框界行；产 `borders`（虚拟界行）+ `line_index`（各列类型） |
| 闸1 | `border_detect_gate` | **`line_detect_gate`** | 产同种 `border_detect_gate_manifest` |
| 2 | `column_warp`：射影 + 剥界行 + 削版框 | **`column_crop`**：裁剪 + 去噪 + 抹长横线 | 没有墙可剥 |
| 闸2 | `column_gate` | 同一道，`count_mode=detected`、`width_tol=0.5` | 列数由 Step1 探出；卷题页列位不等距 |
| 3 | `row_segment`：固定 N 格弹性 DP | **`row_segment_runs`**：墨段 + 字身盒模型 DP | 项数不定、标点挤压、字距随列调整 |
| 4～9 | 复用 | 复用 | 字形库按 `edition` 分域（`modern:<book>`，未开工） |

三处与模式无关的**地基**（`core/`）：pipeline yaml 的 `params:` 段（同一 Step 在两条链里默认
参数不同）；`Pipeline.producer_of` / `RunContext.producer`（同一种产物两个产出者，按当前管线查，
`RunContext` 不带管线时按 `Book.edition` 选默认管线）；`load_book` 优先读工作区 `books/`。

## 二、配置

册 yaml（工作区 `books/<id>.yaml`）新字段，旧册不写等于现状：

```yaml
edition: modern              # 选默认管线与字形库域
writing_mode: vertical-rl    # horizontal-tb 尚未实现
frame: none                  # none → Step1 走 line_detect
script: trad
raw_dir: data_full/bxrl      # 逻辑页（split 产物）
page_split:                  # 有这一段才需要 guji split
  mode: two_rows_hline
  source_dir: data_full/bxrl_scan
expected_cols: 17            # 现代链里是上限，不是等式
pitch_prior: 118.0           # 字距兜底
```

管线 `pipelines/modern_body.yaml`：steps 换前三步，`params:` 给 `column_gate` 两个覆盖。

命令：`guji import-pdf <pdf> --out <dir>`（1:1 抽页）→ `guji split <book>` →
`guji pipeline modern_body <book> --from line_detect --to cell_shrink --pages all`。

## 三、算法要点（判据全部来自北行日錄实测，换书要重看）

**分页 `utils/page_split.py`**：分界三级——实线（整行墨占比 ≥ 0.3）→ 淡线/断线（最长连续墨段
≥ 0.1 页宽；文字行再密连续段也 ≤ 0.03 页宽）→ 白带（≥ 60 行零墨）。**淡线必须排在白带前**：
扫描页 4 的白带在淡线下方，先找白带会把淡线裹进上栏，每列底端多一条细横条、Step3 每列多一项。
栏的上下界由「强正文行段」（跨度 ≥ 0.1 页宽、密度 ≥ 0.003）定，弱段（跨度 ≥ 0.02）只有紧挨
（≤ 150px）强段才收——挡住影印本页码（跨度 0.042、离正文 240px+），收进首列高出一字的首字
（末页「獨見李同年」，跨度 0.026、离强段 22px）。书口竖线先抹，否则每行都「有墨」。

**行探测 `utils/line_layout.py`**：列 = 列方向投影 > 0.008 的段；em = 最宽一族宽度中位；
正文列 0.75～1.35 em；两条相邻窄段拼成一字宽 → 双行小注整列（`jiazhu_pair`，Step3 还切不了）；
贴着正文列且 y 在其内的窄段 → 并回（卷题下的小注子列）；块内 ≥ 0.4 块高的窄段 → 小字整列
（`small_col`，脚注）；**书口小字 `margin` 先于配对判**：长竖线外侧，或块外的**短**段（< 0.35 块高）
且（离块 > 1.5 列距，或不在列位网格上）——卷次/页码/书眉都短，卷题列「攻媿先生文集卷第一百十九」
高，它离块 2.5 列距又不在网格上，只有「高」能救它。列位网格的相位取与最多正文列同相的那列；
列距取中心差的最小族、族须 ≥ 2 个且 ≥ 差数的 1/4。窄（< 0.35 em）、满墨（有墨行占比 ≥ 0.95）、
≥ 0.5 页高的段是**斜了的栏线**（单列墨占比到不了 `long_rules` 的 0.5），补进竖线、不算列。
相邻正文列中心距 ≈ k × 列距时补 k−1 个 `empty` 列位（卷题页），否则两列共用中线、窗口宽一倍。
上下框在块外 1.0 em（Step4 extractor 在窗口底沿 ±40px 找框线行，余量不够首末字横笔就被当框线；
但余量的上限是分页留白，见 §五.3）。

**切分 `utils/run_segment.py`**：按纯白缝切墨段；小于 0.55 em 且靠列右半的小块 = 标点形态；
DP 把连续墨段合成项（跨度 ≤ 1.15 em，且 ≥ 单段最高——em 估小时不至于无解），代价 = 相邻
项中心距偏离本列 pitch（字–字紧、标点两侧松）。`一` 自成一项：并进邻字会让中心距塌成半个
pitch。冒号「︰」两点合一个标点项（都是靠右小块、合跨度 ≤ 0.6 em）。**两遍法**：先每列自估
em/pitch，取页中位；再拿页级值当 hint 重切每列——小字多的列（人名注「張說、張掄、宋鈞」连排）
自估 em 偏小到 87（字身 104），DP 无解退化成「每段一项、无标点」；短列（「二月」）自估不出。
脚注列（small_col）不套页级值。

## 四、北行日錄实测（2026-09-14）

工作区 `D:\workspace\beixingrilu-workspace`（`GUJI_WORKSPACE`），册 `bxrl`：樓鑰《攻媿集》
卷一一九～一二〇《北行日録》现代排印本影印，40 张扫描页 3888×6000（约 600dpi，1-bit），
每张上下拼两页原书，栏间横线；字身 ≈103px、列距 185.5px、每栏 ≤17 列、每列 14～16 汉字 +
2～4 标点（标点挤压、字距 111～132px 随列调整）；卷题下双行小注 1 处、单行小字人名注 46 处、
脚注 ①② 十余处（栏内左端小字窄列）。用户给的校對本 2.4 万字（一行一列、影印页码分块）当证人。

**Step1–3 与校對本对账**（工作区 `scripts/witness_check.py`，按影印页码映射；两边都跳过脚注；【】合并）：

| 指标 | 值 |
|---|---|
| 扫描页列数 + 页内总项数与校對本全等 | **29 / 40**（2026-09-15 分页余量改后重跑仍 29） |
| 全书总项数 | 22,139 vs 校對本 22,131（+0.04%） |
| 逐列项数相等 | 1,244 / 1,309 = 95.0%（含校對本自身的换行错位） |
| 全链 Step1→3 单页耗时 | ≈ 0.5 s（Step4 另 1 s） |

**剩下的不等，逐条看过图，绝大多数是校對本的噪声**：换行错位（p39 c13/c14「虜」在图上第 13
列末，校對本放到下一行）、小注前后的「、」「。」被省略（「周邵州、伯駿。」「正字，仲友。」）、
漏字（「甲人依」少「人」）。**我们的已知短板**：挤压成一格的「」「（几何上与「二」无法区分，
p36 c12）；「？」被切成钩+点两项（全书 2 处）；卷题页的双行小注整列切不出来（1 处）。

## 五、Step5：证人标签、字体判定、播种（2026-09-15）

三条新命令，都在 `cli_v2`：

| 命令 | 做什么 | 北行日錄实测 |
|---|---|---|
| `guji witness-align <book>` | 列级证人对齐（`utils/witness_align.py`）：整理本一行 = 一列，Step3 项数与该行字符数相等的列，字位与字符一一对应 → `products/<book>/witness_align/labels.jsonl`。项数不等的列整列不给（不猜） | 1,234/1,309 列对齐 → 20,852 个字位标签 |
| `guji calibrate-font <book> [--manifest … --norm-stroke 3]` | 字体判定（`utils/font_calibrate*.py`）：标签字位在各套字体里检索，recall@1/@5、正确/错误命中 cov 分布、可分性 margin | 见下表 |
| `guji seed-witness <book> --fonts font:iming,font:simsun` | 播种（`utils/seed_witness.py`）：证人字 × 任一字体 top-1 一致 → `admit_instance(provenance="align", edition="modern:<book>")`，来源 `kind=print`。**两阶段**：先全部检索再全部进库——边检索边进库会让 `GlyphDB.query` 的特征缓存每条失效，25 分钟只进 4,101 条 | 17,269 字位 → 进库 16,279（两套都认 14,013、只一套认 2,266）、字体不认 962（5.6%）；1,976 字头 |

### 五.1 字体判定：三种量法，结论一致

候选七套（工作区 `config/fonts/manifest.json`，本书 2,091 字表）：仓内 I.Ming、Jigmo，本机
細明體 MingLiU、宋体 SimSun、Noto Serif TC/SC，另加用户指定的**中华书局宋体**
（github xiangrongjingujiu/ZhongHuaSongFont，古联委托方正制作，三平面 13 万字，本书字表全覆盖）。

| 量法 | I.Ming | SimSun | Noto SC | Noto TC | MingLiU | Jigmo | 中华书局宋体 |
|---|---|---|---|---|---|---|---|
| 经库、原样渲染（recall@1） | 0.915 | **0.917** | 0.869 | 0.854 | 0.827 | 0.832 | 0.818 |
| 直接比对、两边细到 2px | **0.939** | 0.929 | 0.911 | 0.915 | 0.920 | 0.907 | 0.876 |
| 直接比对、两边细到 3px | **0.948** | 0.926 | 0.917 | 0.925 | 0.926 | 0.907 | 0.874 |

三个教训：

1. **原样渲染那一行是在量粗细**：书上字块归一到 64px 后笔画 5.5px，SimSun/I.Ming/Jigmo 渲染
   3.8px，中华书局宋体/MingLiU/Noto 2.7px，排名与笔宽严格同序（`utils/font_calibrate_direct.py`
   `stroke_width_px`）。匹配栈 2026-08-24 起「不做笔宽归一」是给刻本定的，现代印刷同字同体，
   粗细差反而是主噪声；
2. **把字体加粗到书的笔宽再比是假象**：七套字体正确/错误命中 cov 全部 0.998～1.000、刻本 same 闸
   判 644/675——64px 图上 5px 宽的笔画，差一笔的形近字在软覆盖（τ=1.5、块内 ±1px）下也几乎
   全覆盖，比对饱和，只剩 HOG 粗排在起作用；
3. **两边骨架化再统一细化到 3px** 才是在量字形：I.Ming 第一、SimSun 第二稳定；中华书局宋体在
   三种量法下都垫底——**这本书不是用它排的**（它 2020～21 年才造，本书排印年代更早，是铅字/照排
   宋体谱系，SimSun 与之同源）。

七套字体的 margin（p10 正确 − p90 错误）全部为负（细到 3px 时 SimSun −0.003、I.Ming −0.007），
与方案 §一.2 的预判一致：**字体只当候选源与播种的形状证人，不当精确判据**。选定
**I.Ming + SimSun**（前二、字表全覆盖；I.Ming 是自由字体可随仓库走，SimSun 是 Windows 系统字体
只在本机用），其余五套已 `glyph-db drop-edition` 删掉。

### 五.2 书自己的库能不能当精确判据：能，但必须笔宽归一

同书留一法（工作区 `scripts/self_match_bench.py`，模板 = 证人标签字位，查询摘掉自己）：

| 归一 | recall@1 | cov 正确 p10/中位 | cov 错误 中位/最高 | 刻本 same 闸（0.996） |
|---|---|---|---|---|
| 不做笔宽归一（12k 模板、800 查询） | 0.9938 | 0.9993 / 0.9995 | 0.9875 / **0.9996** | 794 对、**2 错**（却→卻、内→內） |
| 骨架化细到 3px（6k 模板、600 查询） | **0.9983** | 0.9992 / 0.9995 | 0.9220 / **0.9220** | 591 对、**0 错** |

所以 `modern_body.yaml` 给 `glyph_match` 两个参数：`norm_stroke: 3`（`load_matcher_from_db` 从
canonical 图块现算带笔宽归一的 norm，不用库里存的 `derived.norm`；查询侧同法）、
`exclude_self: true`（库就是这本书自己的字位，不摘自己就是自证 cov 1.0）。Step5-a 在现代书上
`edition` 缺省 = `modern:<book>`。刻本链两个参数都是缺省值，行为逐位不变——但 `GlyphMatchParams`
多了字段，刻本 Step5-a 产物的 params_hash 会变、显示过期，重跑结果相同。

### 五.3 Step5-a 全书结果（工作区 `scripts/glyph_match_check.py` 对账证人标签）

| 指标 | 值 |
|---|---|
| 字位 / 有证人标签 | 22,197 / 17,419 |
| 档分布 | same 14,109 · unsure 4,428 · diff 3,660 |
| same 档覆盖（有标签的） | 13,283 / 17,419 = **76.3%** |
| same 档与证人一致 | 13,242 / 13,283 = **99.69%**（41 个不一致） |
| 其中同形异码 | 32：強/强 ×4、內/内 ×9、稅/税 ×3、換/换 ×3、戶/户 ×2、搖/摇、衞/衛、卻/却…——书上只印一种形，证人两种码都用了，库被播成了两个字头，谁先命中算谁 |
| 其中真形近误判 | **9**（鐘→鍾、揚→楊、贏→嬴、旦→且、字→宇、宣→直、內/内 之外的）= 9 / 13,283 = **0.07%** |
| 单页耗时 | 库 16k 字块建索引（含笔宽归一现算）≈ 90 s 一次；逐页 ≈ 12 s |

**41 例已人裁**（审查页 https://claude.ai/artifact/2B2id71yUuaneP8hGdUJYR，2026-09-15）：
**36 例校對本录错、5 例库匹配错、同形异码 0**。前面「同形异码」的猜测不成立——书上只印
内/强/税/换/户/却/吕/衛/摇/澁/嬴/鍾/春 这一种，校對本在这 36 处录成了另一个码或另一个字
（強/內/稅/換/戶/卻/呂/衞/搖/澀/贏/鐘/舂），订正清单在工作区
`products/bxrl/witness_align/witness_corrections.md`（overview 仓 `报告/` 有副本）。5 例真错是
字/宇、宣/直、揚/楊、旦/且、吴/吳——放大原块核实书上都是校對本那个字，cov 0.996～0.999，
same 闸分不开；而且库里 字 0 个样本、宇 4 个，旦 0、且 9：**字体模板同样分不清这几对，播种时把
书上的 字/旦 全拒了**，于是每个 字 只能去配 宇。与刻本链「形近字对在任何全局相似度下分布重合」
（`g3g4_error_analysis.md`）一致，处置也一样：这 5 对进 `config/confusable_human.json`，
护栏把它们压成 `unsure` 交 Step6 上下文。按人裁后的真相，Step5-a same 档精度 = 13,278 / 13,283
= **99.96%**，真错 0.04%。

**宣/直 那例还牵出一个切分线的 bug**（用户看审查页发现「应为」样本是截断的「宣」）：库里三个「宣」
全是**列末字**（slot 18），底横都没了、Step4 打的 `boundary_ink`。全书 `boundary_ink` 102 个字位，
82 个在列末，**78 个进了库**。追根追了三层：

1. Step4 extractor 在窗口底沿 ±40px（`FRAME_HINT_TOL`）里找「框线行」并钉桩、又在末格端区抹
   「版框横条行」（`mask_frame_bars_outside`）——宋体「宣/亘/血/皿」的底横是独立连通块、宽近满格，
   落进这 40px 就被当版框线；
2. `line_detect` 的上下框余量 0.6 em 提到 1.0 em **没用**（重跑后 `boundary_ink` 仍 102）——
   因为**逻辑页图本身只在正文块外留了 30px**（`page_split.pad=30`），窗口撑不出图外，列末字离图底
   只有 31px；
3. 于是真修在分页：`page_split.pad` 30 → 130（≥ 一个字身再多一点）。顺手把 extractor 的端区抹横条
   做成开关（`CharExtractor(frame_guard=)` / `CellShrinkParams.frame_guard`，刻本默认开、
   `modern_body.yaml` 关），`seed-witness` 排除带截断类旗标（`boundary_ink / truncated /
   contaminated / frame_bars / bad_seg / rule_bar / edge_blob`）的字位——与刻本链排除名单 r3 同一条纪律。

三处改完 split → Step1–5a 全链重跑、库重播（数字见下）。教训：**逻辑页是管线的原图，分页时的
留白就是下游一切「余量」的上限**；自动评测看不出截断样本进库（它是「一致地错」），是用户在审查页
看到「应为」样本被截断才抓住的。

余量一变又露出两个连带问题（同日修）：**`line_detect` 列投影的绝对门槛对短列不稳**——
`colfrac` 按整幅高平均，多出 200 行白后「二月」这种两字短列的边缘笔画掉到 0.008 之下，列宽从
100px 缩成 72px、被判成小字列，整页列数少一；改成迟滞（范围按 col_ink_min/4 取、成立要求峰值
≥ col_ink_min）。**`witness-align` 只逐列比项数不比页级列数**——一列漏探后，后面每一列都对上
邻列的那一行，满列都是 18 项、项数照样相等，整页标签错一列（p70 一页凭空多出 50 个「错」）；
改成页级列数不等整页不给标签。

第二轮连带（同日）：书口判据先于配对后，又有三处露出来——**书口栏线斜了** `long_rules` 抓不到
（20px 宽 2200px 高的线歪 1° 就横跨 40px，没有哪一列墨占比到 0.5），它成了一个「小字列」，与书眉
小字的中心距 59px 成了列距的**最小族**（族里只有它一个），p7 列距 185 → 58、整页插满 35 个空列位；
**卷题列被判成书口**——「攻媿先生文集卷第一百十九」离正文块 2.5 列距、不在网格上，按「远 / 离网格」
两条都像书口，扫描页 1、27 各少一列、整页不给标签。修：窄而满墨、≥ 0.5 页高的段直接当竖线；
列距最小族须 ≥ 2 个且 ≥ 差数 1/4；只有**短**段（< 0.35 块高）才用「远 / 离网格」判书口。
重跑后回到 29/40，逐列相等 1,244/1,309 = 95.0%（原 94.3%），证人标签 20,994 个（原 20,852），
`boundary_ink` 102 → 10。

### 五.4 五例「真形近误判」的根因：库里根本没有那个字（2026-09-15）

用户追问「我以为匹配算法是 100% 准的，为什么 4 个匹配成不同的字」。追下去，**这五例的根子
不在匹配器，在播种闸把正确的字整批拒了**——重播后的库里 `字` 0 个样本、`宣` 0、`旦` 0，
`揚`/`吴` 各 1（留一法摘掉自己 = 0）。书上的 `字` 只能去配 `宇`，`宣` 只能配 `直`。
两个 bug 叠在一起：

1. **播种闸走的是不归一笔宽的老协议**。`seed_from_witness` 调 `db.query`（库路、原样渲染
   模板），而 Step5-a 走 `norm_stroke=3`。不归一时书块 4.6px vs 字体 2.7px，cov 0.6～0.8
   量的是粗细：全书**每一个** `字` 的字体 top-1 都是 `宇`、每一个 `旦` 都是 `且`，两套字体
   一致地反，于是一个都没进库。修：`seed-witness --norm-stroke 3`，与管线同一把尺子。
2. **字体模板本身被毁了**。`normalize.remove_edge_specks` 的规则 2「贴边细线 = 版框/界行」
   前提是「版框 2～5px、本书笔画 8～14px 两个分布不重叠」——那是**刻本切分块**的经验，对
   **字体渲染**不成立：256px 画布上宋体横笔只有 7px（thin_cap 11.5），于是 `旦` 的底横、
   `宣` 的宝盖和底横、`二` 的上横被当版框删了。库里的字体 `旦` 是个「日」、`宣` 是个「亘」。
   I.Ming 全字表 2091 字有 **84 个**中招（旦宣二亘立高京音意齊豆登丞云…），书上的字块抽
   3000 个**一个没中**——只有字体这一路坏了。修：`normalize_patch(strip_lines=False)`，
   字体渲染一律不删细线（`font_glyphs` 入库、`load_matcher_from_db` 现算、
   `font_calibrate_direct` 模板侧三处）。

修后书上的字对本字模板反超形近字（I.Ming，笔宽归一 3px）：

| 书上的字 | 对本字模板 | 对形近字模板 |
|---|---|---|
| 宣 | 0.995～0.998 | 直 0.960～0.967 |
| 旦 | 0.9998 | 且 0.993～0.999 |
| 字 | 0.990～0.997 | 宇 0.987～0.994 |

**再往下是这把尺子的天花板，要记住**：骨架 3px + 弹性覆盖对「差一笔」的字对分辨率只有
千分之一到千分之五。书内互比：同字自比 0.9992～0.9997，而 `字/宇` 0.994（最高 0.998）、
`旦/且` 0.998、`宣/直` 0.991（最高 0.998）、`揚/楊` 0.9965——same 档门槛 0.996 **分不开**。
能分开的是**排序**：库里有本字时本字排第一；**库里没本字时，形近字照样过门槛、而且没有对手，
看起来很自信**。所以「库缺字 = 必错且高分」是这条链的结构性风险，形近字对必须留
`confusable_human.json` 护栏 + Step6 上下文，且播种的拒绝名单要当一等公民盯（见 §六）。

另两例要改判：**`吴/吳` 不是匹配错**——书上两处同一个字形（互比 0.9993），是校對本把同一个
人名录成两种码，归到 36 例校對本错那一类；**`揚/楊` 是留一法协议的产物**——全书只有 1 个
`揚`，摘掉自己库里就没有了，真上线时新页的 `揚` 会配到这个样本（0.996 vs 楊 0.991）。

### 五.5 训练 / 测试切分（用户 2026-09-15 定）

**逻辑页 21–80（扫描页 11–40）= 训练集**，人裁结果可回流（护栏、校對本订正、库修正、
阈值标定）；**逻辑页 1–20（扫描页 1–10）= 测试集，不做任何人工干预**。
界线划在**人工干预**，不是库成员：库照旧全书播种、Step5-a 照旧 `exclude_self` 留一法，
只要 1–20 没有人看过、没有人据它改过代码或库，在它上面测出来的就是真实上线水平。
规模：Step4 字位 训练 16,662 / 测试 5,519；证人标签（正文字）训练 13,196 / 测试 4,193。

### 五.5b 修完两个 bug 后的全书结果（2026-09-15 晚，库重播 + Step5-a 全书重跑）

播种：字位 17,379 → 进库 **17,153**（字头 2,010），字体不认 **213**（修前 971）。
库里 `宣` 0→30、`字` 0→6、`旦` 0→5 个样本——**五例「真形近误判」全部消失**。

| 指标 | 训练（21–80） | 测试（1–20，无人工干预） | 全书 |
|---|---|---|---|
| Step4 字位 / 有证人标签 | 16,662 / 13,331 | 5,519 / 4,208 | 22,181 / 17,539 |
| same 档覆盖 | 79.3% | **78.3%** | 79.1% |
| same 档与证人一致 | 99.697%（32 例） | **99.848%（5 例）** | 99.733%（37 例） |
| 护栏拦下（unsure） | never_match 2,066 · conflict 95 | never_match 709 · conflict 52 | 2,775 · 147 |

**测试段 5 例全是已裁定的「校對本录错」那一类**（強→强 ×3、鐘→鍾、贏→嬴，三对都在
昨天的订正清单里；库里 強/鐘 各 0 个样本、书上只印 强/鍾）。**没有一例是新的形近误判**——
对照昨天同一档的 41 例里有 5 例真错，这次 37 例里 **0 例真错**。

剩下 37 例的字对全部是「书上只印一种形、校對本用了另一个码」：內/内 ×11、稅/税 ×4、
強/强 ×4、戶/户 ×3、墙/墻 ×3、卻/却 ×2，以及 搖/摇、衞/衛、嬴/贏、换/換、舂/春、澀/澁、
呂/吕、鐘/鍾 各 1～2。**这些不是错误，是证人噪声**，真实 same 档精度 = 100%（待人裁复核）。
处置照旧走异体归一层，不进护栏。

**播种被拒的 213 个字位**（修前 971）值得盯一眼：`入` 16 个、`黃`/`曰` 各 4、`一`/`宮`/`強`
各 3——`強` 被拒是对的（书上印的是 `强`，证人给的码是 `強`，字体 top-1 自然不认），
其余多半是切分或字形本身有问题，是下一轮质检的入口。

### 五.5c 第二轮人裁结果（32 例，2026-09-15 晚）

**29 例校對本录错、3 例匹配错、0 例同形异码。** 三例真错：`嬴→贏`（p29c16s14）、
`换→換`（p65c14s6）、`舂→春`（p70c6s10）。

**三例的机制与第一轮不同，值得单记**：不是播种拒了本字，而是**本字在库里只有一个样本、
就是被审字位自己**（嬴 1、换 1、舂 1），留一法一摘库里就没有本字了。`换/換` 两个字位
互为对方的命中样本——书上同一个字形，校對本在 p65 处录成 `换`，而库里那个孤零零的
「换」正是 p65 自己。所以「库缺字必错」有两种成因：**播种拒掉**（可修，见 §五.4）和
**全书孤例**（不可修，是数据的极限）。后者只能靠护栏 + Step6 上下文。

| 分段 | same 档名义精度 | 人裁后真实精度 | 真错 |
|---|---|---|---|
| 训练 21–80 | 10,544 / 10,576 = 99.697% | **10,573 / 10,576 = 99.972%** | 3 例 |
| 测试 1–20 | 3,288 / 3,293 = 99.848% | 5 例字对（強→强/鐘→鍾/贏→嬴）均在订正清单、库里 強/鐘 0 样本 | 待判，预计 0 |

护栏新增三对（`嬴贏`/`换換`/`舂春`，round `bxrl_same_mismatch_r2`），连同
`字宇`/`宣直`/`旦且` 共 6 对来自本书。订正清单合并两轮 = **44 例校對本录错**
（工作区 `products/bxrl/witness_align/witness_corrections.md`）。合并时丢掉 1 条第一轮的
陈旧记录（p58c13s1，切分线修好后该字位已不存在）——**人裁产物会被上游改动作废，
合并时必须拿当前标签复核，不能直接并表**。

### 五.6 控制台对现代链的支持（2026-09-15 补齐）

三处缺口，都已修：

| 缺口 | 症状 | 修法 |
|---|---|---|
| 前端 Step 清单只认刻本链 id | 现代书每一步进度条 0/80、Step 页「没有产物」 | `steps.ts` 加 `modernId` + `backendIdFor(meta, edition)`；`Book.to_dict()` 带出 `pipeline`，两页按册书取管线（此前硬编码 `keben_body_v2`） |
| 5-a 调试视图写死裸默认参数 | 台上用的是**不归一的全库**比对，与管线跑的不是同一次匹配——人照着台上的证据判，判的是幻觉 | `_params_for_book()` 读管线 `params.glyph_match`，把 `norm_stroke` / `edition` 传进 matcher 与归一 |
| 叠图 | —— | `render/overlay.py` 早已支持三个新 Step，无需改 |

## 六、没做的与下一步

- 横排：原图入口旋转 + `to_original()` + 叠图逆旋转（方案 §三），没动；
- `」「`/`？`/双行小注整列：留给 M3；
- 标点字位未播种（`seed-witness` 只收 `kind=char`）；
- 分页的 manifest 记了逻辑页 ← 扫描页的矩形，Step9 回扫描页坐标时用；
- 控制台前端未认识新 Step id（`line_detect`/`column_crop`/`row_segment_runs`），CLI 叠图
  （`guji product overlay`）已支持。
