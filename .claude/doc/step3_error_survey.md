# Step3 切分错误普查（2026-09-03）

对 v2 链 Step3（单列文字切分）当前的失败做一次穷尽分类。结论先行：

> **Step3 自身的算法没有问题。** dev_set 与 vol02 全书范围内，所有「无解」与「切不到」
> 都能归因到**上游给的参数或列图不对**，没有一例是 DP 本身切错。

量法：`guji-cv pipeline keben_body_v2 <册> --pages all --to column_gate` 扫全书，
再逐列跑 `segment_column`，对无解的列换参数复算、看列图、量边缘墨。

---

## 一、总账

| 范围 | 列数 | 有解 | 失败 | 失败构成 |
|---|---|---|---|---|
| vol01 dev_set（换页前） | 108 | 98 | 10 | 职名页 9 + 单列 1 |
| **vol01 dev_set（换页后）** | 108 | 98 | 10 | **上游闸拦下 9（p42）+ 单列 1（p60 c8）** |
| vol02 dev_set（12 页） | 108 | 81 | 27 | 全部被上游闸拦下（3 页 × 9 列） |
| **vol02 全书**（188 页） | — | — | — | **过闸 159 / 188 = 84.6%** |

换掉职名页之后，**两册的失败构成变成同一个原因**：上游交接闸按列宽整页拦下。
这才是正文页的真实表现，也说明该修的就一处（乙类）。

vol02 全书过闸页的页级周期 `period` **中位 113、范围 107~118，无一页低于 90**
——周期估计在正文页上是稳的。

---

## 二、三类失败，逐类定性

### 甲类：职名页用了正文的格数先验（vol01/119，9 列全灭）

**不是弯页，不是图坏。** 列图干净、字清晰，问题在参数：

| 页 | 页型金标 | 页级 period | 每列字块数 | 间距中位 |
|---|---|---|---|---|
| vol01/24 | body | 116 | 26 | 106 |
| vol02/133 | body | 112 | 21 | 106 |
| **vol01/119** | **roster** | **70** | **7~19（逐列不同）** | **85~372（逐列不同）** |

正文页字距稳定在 106 上下，自相关有一个干净强峰（0.541）。职名页字大稀排，
自相关最强峰只有 0.197 且分裂成 98 / 108 / 119 三个弱峰——**页内字距本身就不均匀**，
`estimate_period` 于是锁到了 2 倍谐波上，估出 70。

拿「每列 21 格 + period 70」去切一列只有 7 个大字的页，DP 必然无解。**换对参数就全好**：

| 参数 | 结果 |
|---|---|
| `n=21, period=70`（现行） | 9 列全部无解 |
| `n=8, period=300` | 9 列各 8 格 |
| `n=7, period=340` | 9 列各 7 格 |
| `n=6, period=400` | 9 列各 6 格 |

⚠️ **但不能简单给职名页换一套固定先验**：vol01/119 九列的字块数是 14/19/10/17/7/10/11/12/8
——**逐列都不同**。这正是 `layout_spec` 里 `elastic`（弹性列）的定义：字距 ≠ 1×格高。
职名页要么按列自估格数，要么根本不进 Step3。

**当前策略下这不是 bug**：项目 2026-08 起「只优化正文页」，vol01 的 44 页职名、
47 页目录本就不该进正文管线。**真正的缺陷是 dev_set 里混进了一页职名页**
（vol01/119），让它每次都报 9 列失败，掩盖了真实的正文页表现。

### 乙类：最外列残留整条界行，列宽超闸（vol02 全书 19 页）

vol02 全书 27 页因「列宽偏离中位数 ±15%」被拦，其中 **17 页只有 c1 一列偏**，
偏离量清一色 +18%~+29%。看列图与边缘墨：

| 页 | c1 宽 | 右缘外 12% 墨占比 | 目视 |
|---|---|---|---|
| vol02/46（正常对照） | 187 | 0.003 | 干净 |
| vol02/51 | 237 | **0.193** | 右侧一条斜界行贯穿 |
| vol02/109 | 240 | **0.250** | 同上，更明显 |
| vol02/45 | 239 | 0.061 | 断续残留 |

**根因在 Step1**：最外侧那条界行是斜的，探测时位置定偏，把线本身圈进了列窗。
分布上看是**尾部问题不是系统偏移**——c1 宽比中位的比值中位数 1.011（正常），
但尾部 19 页超 1.15；对侧 c9 只有 3 页超。

**闸拦对了**：这种列图带着一条整界行进 Step3，切出来的格必然含大片非字墨。
但**代价是整页 9 列全部作废**，而其实只有 1 列坏。vol01/42 是最干净的例证：

| 列 | 宽 | 偏离 | 外缘墨 |
|---|---|---|---|
| c1~c8 | 180~192 | −3.7%~+2.7% | ≤0.008（全部干净） |
| **c9** | **218** | **+16.6%** | **左缘 0.092（残留界行）** |

八列完好、一列坏，结果整页 9 列作废。同一页的 c1~c8 本可以直接用。

### 丙类：单列真失败（vol01/60 c8，1 列）

dev_set 里唯一一例「正文页、过了闸、但该列 DP 无解」。占正文页列数的
1/(108−9) ≈ **1.0%**。属于个案，未深究。

---

## 三、几条可以立刻做的事

按性价比排：

1. **把 vol01/119 从 dev_set 换掉**（换一页 body）。它是职名页，每次贡献 9 列假失败，
   让 dev_set 的「98/108」看起来像 91% 而实际正文页是 98/99 ≈ 99%。
   改 `books/vol01.yaml` 一行的事。
2. ~~**交接闸改成列级放行而不是整页作废**~~ ✅ **已做（2026-09-03）**，见下节。
3. **给职名页单独的 pipeline**（`keben_roster.yaml`），格数按列自估而非固定先验。
   这是「新版面 = 新 yaml + 缺的那几个 Step」的第一个真实用例。
4. Step1 的斜界行定位（乙类根因）留给算法层，见
   `segmentation_v2_pipeline.md` 的「下一步 a）修 Step 1 的竖直线/列探测」。

---

## 四、已做的修复：交接闸改列级放行（2026-09-03）

**改了什么**：`column_gate` 里「列宽偏离本页中位数超 ±15%」这条判据，
从**页级拒因**移到**列级拒因**（新代号 `L1c`）。它本来就是逐列算得出的，
放在页级等于一列坏就整页作废。页级 L1 现在只留「探出的列数对不对」——
那才是真正整页性的问题（列编号和窗口一起错位，单列救不回来）。

**顺带改对的一处**：页级共享量 `period` / `ref_w` 原先用**全部列**算，
现在改用**几何正常的列**（剔掉 L1c 命中的）。宽度异常的列多半把界行圈进了列窗，
它的投影带着一条整线，不该参与共识。实测剔掉后 period 变化 ≤1.5、ref_w ≤5px，
七个受影响页全部落在健康区间（period 108~115）。正常列不足半数则整页拒绝。

**实测收益**（vol02 全书 188 页 / 1682 列）：

| | 改动前 | 改动后 |
|---|---|---|
| 页级过闸 | 159 页（84.6%） | **186 页（98.9%）** |
| 可用列 | 1431（159×9） | **1624（96.6%）** |
| dev_set 列有解率 vol01 | 90.7% | **98.1%** |
| dev_set 列有解率 vol02 | 75.0% | **85.2%** |

**净增 193 列（+13.5%）**。剩余被拦 50 列，原因分明：页级列数不对 8、
L1c 列宽 30、L2 边缘墨 20。抽查确认都是真问题，无误拦：

- vol02/177 整页九列几乎全被拦（外缘墨 0.10~0.28）——**这页根本没有印刷界行**，
  探出的红线全部斜穿在字上，列窗本就不可信，拦得对。
- vol01/42 现在只拦 c9（宽 +17%、外缘墨 0.092），c1~c8 照常放行。

回归：`tests/test_column_gate.py` 4 条，钉住「列宽只出现在列级、页级拒因里不许再有」
与「剔掉异常列后页级先验仍在健康区间」。

## 五、这次普查用到的量法（可复用）

- **判周期估得对不对**：看自相关峰的**高度与个数**。正常页一个强峰（≥0.5），
  病态页多个弱峰（≤0.2）。比只看 `estimate_period` 的返回值可靠得多。
- **判列图有没有残留界行**：量两侧外 12% 的**最大**墨占比。干净 ≤0.01，
  残留整条线 ≥0.19。比「带内残墨」可靠——后者是按零墨挑出来的边界，循环论证。
- **判字距是否均匀**：直接数字块中心的相邻间距，看中位与范围。正文页范围窄
  （32~126，中位 106），职名页范围宽（85~372）。

---

## 六、第二轮复查（2026-09-03 晚）：首尾字符被切，两个都是 Step1 的问题

用户提的两件事，查完都成立，且都归因到 Step1。**已投进反馈通道**（见末节）。

### 丁类：抬头列漏检 —— 漏检率 96%

vol01/141 「明显切分有问题」的真相：**它不是切分错误，是排版本身**。

c7 是**抬头列**——「諭旨」按古籍礼制顶格从版框起写，其余八列正文低两格起。
九列 `top_y` 一致（328~343）、band 宽正常（183~195），**Step1 的列窗定位是对的**。
错的是 `raised` 标记：

| 列 | 墨起点（列图 y） | raised |
|---|---|---|
| c1~c6, c8, c9 | 238~287（正文起点） | False ✓ |
| **c7** | **0**（顶格抬头） | **False ✗ 应为 True** |

`raised=False` → `top_slack=0` → Step3 用正文的格位先验切一列抬头字，
首格必然错位（c1/c4 出现两个空格、c7 的「諭」被压在格顶）。

`ColumnWindow.raised` 完全来自 Step1 的 `detect_head_raise()`
（`column_projection.py:489`），Step2/3 只是转发。全书普查：

| | 条数 |
|---|---|
| Step1 探到的抬头列 | 5 |
| **实际抬头但没探到** | **127** |
| **漏检率** | **96%** |

量法：列图带内逐行墨占比 >0.05 的首行，比本页九列中位早 60px 以上即判抬头。
vol01 与 vol02 全书扫描。

### 戊类：下版框贴住末字，切掉末笔 1~16px

「末尾字符被切」也成立，但**程度比预想轻**。把列窗向下延伸 140px 重投影，
量跨越原下界的连通体：

| | 值 |
|---|---|
| dev_set 列中末字墨距下界 ≤2px（贴边） | **25/36（69%）** |
| 末字跨越下界的列 | 16 |
| 切掉像素 中位 / 最大 | **2px / 16px** |

红线（列图下边）下方那条黑带是**下版框线**不是字——`bottom_y` 定位方向没错，
但**贴得太紧、零留白**。真实排版末字与版框之间必有留白，69% 的列只剩 1~2px
说明下界系统性地压在末笔上。切掉中位仅 2px，对识别影响有限，但没有余量，
稍有弯页就会真切进字。建议 `bottom_y` 下移一个安全余量（事件里给了
`suggest_bottom_y = bottom_y + clipped + 4`）。

上边缘对照：dev_set 216 列中顶边触墨 7 列、底边触墨 53 列（24.5%）——
**底边系统性比顶边紧**。

### 已反馈回 Step1（这是本轮的重点）

之前**没有反馈回去**。`.claude/doc/segmentation_border_feedback.md` 记的是
**v1 phase3 格线整体上飘 35~55px**，与 v2 的贴边切不是同一件事；而且它只是散文，
没走 Event 通道。查 `feedback/events/` 发现目录根本不存在——**通道搭好后一条事件也没进过**。

本轮已按 Event/route 机制投递：

```
批次  2026-09-03-step1-boundaries        223 条
  127  head_raise_missed                  抬头列漏检
   96  bottom_border_clips_last_char      下版框切末笔
```

全部 `kind=verdict, target.step=border_detect, actor=model`，经路由表
（`verdict × border_detect`）命中 `gold_add → border-detection/column-split`，
未命中 0 条。payload 带 `anchor`（`raw_page_px@top-right` + top_y/bottom_y）、
量法说明与 `suggest_bottom_y`，产物重生后仍可定位。

**这是反馈通道的第一次真实使用**，也验证了 §3.6 路由表在真实事件上可用。

### 修复优先级

1. **抬头列检测**（丁类）——漏检 96%，直接毁掉抬头列的整列切分，影响最大。
2. **下界留白**（戊类）——切掉中位 2px，当前危害小但无余量，改一个常数的事。
3. 斜界行定位（乙类，见 §三）。
