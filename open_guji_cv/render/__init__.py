# -*- coding: utf-8 -*-
"""产物的**画法**：把某一步的数值产物画回原图上。

叠图是产物语义的一部分（「border_detect 的竖线画成红色折线」是这一步的定义之一），
不是 HTTP 层的事。原先五个 Step 的画法全长在 `console/app.py` 的
`GET /api/cache/….png` 后面（控制台重构方案 §一），搬到这里之后，
云端道也能一条命令出叠图 PNG 交回本地看。
"""
